export type LoginTypes = {
  email: string;
  password: string;
};

export type SigninTypes = {
  name: string;
  email: string;
  password: string;
  confirmEmail: string;
  confirmPassword: string;
};
